# Observatorio de Techos Verdes CDMX

# Modo de datos: 'mock' (default) o 'official'
NUXT_PUBLIC_DATA_MODE=mock

# URL del backend (cercu-backend)
NUXT_PUBLIC_API_BASE_URL=http://localhost:3003/api/v1

# API keys (Gemini, GEE, Sentinel Hub) se configuran en cercu-backend, no aquí.
